# <img src="https://cdn.jsdelivr.net/gh/devicons/devicon/icons/python/python-original.svg" height="40" alt="python logo" /> Python

Este diretório é dedicado ao estudo de **lógica de programação utilizando Python**.  
Os exercícios aqui têm como objetivo desenvolver o **raciocínio lógico**, a **capacidade de resolver problemas** e a **compreensão da base da linguagem**, sem depender de frameworks ou bibliotecas externas.
Veja abaixo conceitos básicos para você poder entender o código:

---

## 🔹 Variáveis e Tipos de Dados
Conceitos fundamentais para armazenar e manipular informações.

- Declaração de variáveis  
  Em Python, **não é necessário declarar o tipo da variável**.  
  O tipo é definido automaticamente de acordo com o valor atribuído.

- Tipos primitivos:
  - `int` (Números inteiros.)
  - `float` (Números decimais.)
  - `str` (Textos, palavras e caracteres.)
  - `bool` (Lógico, sendo somente `True` ou `False`. Verdadeiro ou falso.)
  - `None` (Representa a ausência intencional de valor.)

---

## 🔹 Operadores Aritméticos
Utilizados para realizar cálculos matemáticos.

- Adição (`+`)
- Subtração (`-`)
- Multiplicação (`*`)
- Divisão (`/`)
- Divisão inteira (`//`)
- Módulo (`%`)
- Potenciação (`**`)

---

## 🔹 Operadores Relacionais
Responsáveis por comparar valores.

- Maior que (`>`)
- Menor que (`<`)
- Maior ou igual (`>=`)
- Menor ou igual (`<=`)
- Igualdade (`==`)
- Diferença (`!=`)

Em Python, a comparação leva em conta **o valor e o tipo**, não existindo operadores diferentes para comparação estrita como em outras linguagens.

---

## 🔹 Operadores Lógicos
Permitem combinar múltiplas condições.

- AND (`and`): Retorna verdadeiro apenas se todas as condições forem verdadeiras.
- OR (`or`): Retorna verdadeiro se pelo menos uma das condições for verdadeira.
- NOT (`not`): Inverte o valor lógico de uma condição.

---

## 🔹 Entrada e Saída de Dados
Interação básica com o usuário.

- `input()` (Entrada de dados pelo usuário)
- `print()` (Saída de dados no terminal)

---
