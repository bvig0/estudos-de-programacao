# <img src="https://cdn.jsdelivr.net/gh/devicons/devicon/icons/javascript/javascript-plain.svg" height="40" alt="javascript logo" /> JavaScript

Este diretório é dedicado ao estudo de **lógica de programação utilizando JavaScript**.  
Os exercícios aqui têm como objetivo desenvolver o **raciocínio lógico**, a **capacidade de resolver problemas** e a **compreensão da base da linguagem**, sem depender de frameworks ou bibliotecas externas.
Veja abaixo conceitos básicos para você poder entender o código:

---

## 🔹 Variáveis e Tipos de Dados
Conceitos fundamentais para armazenar e manipular informações.

- Declaração de variáveis
  - `var` (Forma antiga de declarar variáveis. Tem escopo de função, pode vazar para fora do bloco e causar comportamentos inesperados. Evite em código novo.)
  - `let` (Forma moderna para variáveis que mudam de valor. Possui escopo de bloco, tornando o código mais seguro e previsível.)
  - `const` (Usada para declarar uma constante, que são variáveis que não devem ser reatribuidas. Também tem escopo de bloco e ajuda a manter o código mais organizado e consistente.)
- Tipos primitivos:
  - `number` (Números inteiros e decimais.)
  - `string` (Textos, palavras, caracteres.)
  - `boolean` (Lógico, sendo somente true ou false. Verdadeiro ou falso.)
  - `null` (É a ausência intencional de valor. É usado quando algo existe, mas foi definido explicitamente como vazio.)
  - `undefined` (indica que a variável existe, mas nenhum valor foi atribuído a ela. Normalmente aparece por erro, esquecimento ou fluxo incompleto do programa.)

---

## 🔹 Operadores Aritméticos
Utilizados para realizar cálculos matemáticos.

- Adição (`+`)
- Subtração (`-`)
- Multiplicação (`*`)
- Divisão (`/`)
- Módulo (`%`)
- Incremento (`++`) e decremento (`--`)

---

## 🔹 Operadores Relacionais
Responsáveis por comparar valores.

- Maior que (`>`)
- Menor que (`<`)
- Maior ou igual (`>=`)
- Menor ou igual (`<=`)
- Igualdade (`==` e `===`)
- Diferença (`!=` e `!==`)

A diferença entre (`==` e `!=`) para (`===` e `!==`), é que (`==` e `!=`) comparam apenas o VALOR, enquanto (`===` e `!==`) comparam o VALOR e o TIPO da variável.

---

## 🔹 Operadores Lógicos
Permitem combinar múltiplas condições.

- AND (`&&`): Retorna verdadeiro apenas se todas as condições forem verdadeiras. É usado quando todas as regras precisam ser atendidas ao mesmo tempo.
- OR (`||`): Retorna verdadeiro se pelo menos uma das condições for verdadeira. É usado quando uma única condição válida já é suficiente.
- NOT (`!`): Inverte o valor lógico. Se algo é verdadeiro, passa a ser falso; se é falso, passa a ser verdadeiro. É usado para negar ou inverter uma condição.

---

## 🔹 Entrada e Saída de Dados
Interação básica com o usuário.

- `prompt()`
- `alert()`
- `console.log()`

---
